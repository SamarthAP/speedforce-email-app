const TitleBar = () => {
  return (
    <div className="flex h-[30px] select-none items-center justify-center drag-frame">
      <div className="cursor-default text-xs text-muted">Speedforce</div>
    </div>
  );
};

export { TitleBar };
